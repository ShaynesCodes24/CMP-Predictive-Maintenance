# Data

Use this folder for CMP tool data.

- `raw/`: original or generated data
- `processed/`: cleaned feature tables used by models or dashboards

The starter project uses synthetic data so it can be shared publicly without exposing proprietary fab information.

